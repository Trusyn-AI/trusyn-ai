# Scripts

This directory contains utility scripts for local development, docs generation, validation, and release preparation.

Guidelines:
- Keep scripts deterministic.
- Document usage with inline help text.
- Avoid destructive operations by default.
