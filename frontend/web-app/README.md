
  # Trusyn AI User App

  Organization-facing runtime governance dashboard for monitoring agents, threats, policies, and audit activity.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
